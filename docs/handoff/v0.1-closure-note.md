# v0.1 Closure Note · reject 路径 AC 补验

> 性质：v0.1 收尾澄清（非 ADR、非 spec 修改）
> 日期：2026-08-12
> 关联 commit：`caec191`（feat(T4): fix soft scale + tolerant judge parse; e2e verified）
> 关联 spec：`docs/specs/v0.1-skeleton-spec.md` T4（line 229）

## 背景：caec191 的 "e2e verified" 声明

`caec191` commit message 宣称 T4 端到端验证完成。事实复核（2026-08-12，Hermes + Claude Code 双视角）：

- `state/results.tsv` 只有 1 行 accepted（iter 1，final=0.9600）
- `state/hard-1..5.tsv` 全部为 `test_pass_rate = 1.0`——5 轮迭代从未出现 hard dimension 失败
- **T4 AC 的 reject 路径（spec line 229："5 次迭代里至少 1 次 hard 失败 → 触发 reject 路径（D8 hard rule 7），验证 results.tsv 该次不入表"）当时未真正触发**

即："e2e verified" 覆盖了主路径（iter → commit / revert / ratchet），但 reject 分支从未被真实执行过。

## 补验（2026-08-12 13:45）

方法：临时将 `scoring.yaml` 的 hard signal 从 `pytest -q` 改为 `false`（rc=1 必失败），跑 `iter.sh 6`，验证 reject 路径，随后恢复。

执行证据：

```
iter-6: writer('writer') → D:/programming/projects/my project/recursive-tune/demo-target
  hard[test_pass_rate]: FAILED rc=1 → REJECT (rule 7)
iter-6: REJECTED（hard 失败）— 不 commit、不写 results.tsv（rule 7/8）
```

验证结果：

| 断言 | 结果 |
|---|---|
| `results.tsv` 无 iter 6 行（rule 8：accepted-only） | ✅ 仅 header + iter 1 |
| demo-target git log 无新 commit | ✅ HEAD 仍为 800f929 |
| `state/best_score.txt` 不变 | ✅ 0.9600 / 800f929 |
| `state/hard-6.tsv` 为空（rule 7：reject 不计入 final score） | ✅ 0 字节 |
| writer 留痕存在 | ✅ `state/writer-6.log` |

验证后已恢复：`scoring.yaml` signal 还原为 `pytest -q`；demo-target 工作区 clean；无残留 git 对象。

## 结论

spec T4 的 reject 路径 AC 由本验证闭合（等价于在 commit 链上显式标注：reject 路径 AC 由本次补验闭合）。caec191 的 "e2e verified" 应读作：**主路径已验证，reject 分支由 v0.1 收尾补验（2026-08-12）闭合**。

## 遗留

- `state/` 下保留验证产物（hard-6.tsv 空文件 / writer-6.log / hard-6-test_pass_rate.log），作为 reject 路径的可审计留痕，不清理。
- reject 时 writer 的工作区改动由下一轮 iter 的 D8 rule 11（checkout best）天然覆盖；本次验证中 demo-target 已恢复 clean。

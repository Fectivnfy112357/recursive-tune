# v0.4 Cleanup Notes · T1 债（CONTEXT 漂移 + 死代码）

> 状态:v0.4 收尾 T1 ticket 笔记（2026-08-13,v0.3 first cut 收尾 PR #14/#15 后）
> 锚点:T1 债 1 张 PR（不开新 spec,只清理）
> 范围:**只动** §适用边界 v0.2 段 + `scripts/t2b_*.py` 三个文件

---

## 1. 范围与决策

按用户 grill 共识,3 件 v0.4 清理只做 T1 债:
1. **修 CONTEXT.md §适用边界 v0.2 漂移** — v0.3 spec 第 8 行已声明的已知漂移
2. **删 `scripts/t2b_*.py` 死代码** — `docs/handoff/v0.3-real-e2e-evidence.md` 不引用脚本路径

约束:
- 不动 v0.3 spec / ADR / issue
- 不写新 spec、不引入新骨架件
- "真死代码才删,别碰其它文件"

---

## 2. CONTEXT.md 改动

§适用边界 v0.2 段从"启用/上车/落地"叙事改为与 v0.2 spec D5/D8 + ADR-005 决策 1 实然对齐:

| 旧 | 新 |
|---|---|
| 启用扩展 3 件（Worktrees / Plugins / Sub-agents 派发） | 定义扩展 3 件接口（A 类必不装;C 类按 ADR-005 决策 1 trigger 矩阵判断,v0.2 first cut 不启用——只写接口规格,具体字段层留口 v0.3+ 撞 trigger 时再补） |
| 上车 C 类真实 Target（评估后决定） | C 类真实 Target 仅做上车评估 + 接口定义,实际实例化留给 v0.3 |
| meta 层落地可执行骨架 | meta 层只跑两维 hard（ADR 模板完整度 + 模板占位符替换成功率,见 `scripts/meta_check.sh`）,完整骨架不在 first cut 范围 |

§v0.3 段未动 — v0.3 spec 第 8 行声明的漂移只命中 v0.2 段。
§不覆盖 段未动 — 与漂移无关。
§后续 TODO 段未动 — ADR-005 第 5 条 (启用条件) 仍标 ✅（ADR-005 本身已落 commit `5501711`,CONTEXT §后续 TODO 是索引位,不是"启用条件那行"的措辞来源）。

---

## 3. t2b_*.py 删除判定

grep `docs/handoff/v0.3-real-e2e-evidence.md`:**无 t2b_*.py 路径引用** → 按规则删。

### 引用关系 grep 全集

| 文件 | 引用 | 性质 |
|---|---|---|
| `docs/handoff/v0.3-real-e2e-evidence.md` | 无 | — |
| `docs/handoff/v0.3-T2b-notes.md` | `t2b_one_run.py` / `t2b_aggregate.py` / `t2b_wall_clock.py` | 过程文档历史事实记述（§T2b 落了什么段列了引入脚本清单） |
| `tests/test_t2b_real_e2e.py` | `t2b_wall_clock.py`（docstring 第 7-8 行 + 第 108 行） | 文字注释,非 import;test 默认 skip,不影响 CI |
| `scripts/` 其他文件 | 无 import t2b_* | — |
| `recipes/*.yaml` | 无 | — |

### 删除的 3 个脚本

| 文件 | 用途 | 现状 |
|---|---|---|
| `scripts/t2b_wall_clock.py` | 一次性跑 6 runs 版 | **已废弃**（v0.3-T2b-notes §13 明文标注） |
| `scripts/t2b_one_run.py` | atomic per-run helper | T2b 已跑完,evidence 固化 |
| `scripts/t2b_aggregate.py` | 算 median + 写 evidence markdown | T2b 已跑完,evidence 固化 |

### Ghost reference（已知留口,本 ticket 不修）

- `tests/test_t2b_real_e2e.py:7-8` + `:108` 的 `t2b_wall_clock.py` 注释引用 → 删脚本后注释指向不存在文件
- `docs/handoff/v0.3-T2b-notes.md:11-13` 列出 `t2b_*.py` 引入清单 → 历史事实,文档不构成 ghost（描述的是 T2b 当时的产出,不是脚本当前的可用性指针）
- 两处均不影响 test 运行 / 不影响 evidence.md 数据 / 不影响 git 历史（git log 仍可查 commit `14eefb2` 里的脚本）
- **下个 cleanup ticket** 可顺手处理:test docstring 改成"那是 wall clock evidence 文档的责任",T2b-notes 加一句"脚本 v0.4 cleanup 已删,git 历史 commit `14eefb2` 可查"

---

## 4. 验证

- `grep t2b_` 全仓仅剩 ghost reference(test docstring + T2b-notes 历史记述),无 import / 无运行依赖
- `pytest` 默认环境（无 hermes + 无 T2B_REAL_HERMES）= 111 passed + 3 skipped（与 v0.3 final summary §1 一致,本 PR 无回归）
- `scripts/meta_check.sh`（CONTEXT 是 meta 模板产物的源材料,但本 PR 只动 §适用边界,不动 ADR 模板 + 不动模板文件,meta_check 应不受影响）

---

## 5. 后续

- PR #16（T1 债）:开 PR **不 merge**,等用户 ack（沿用 v0.3 final summary §3 的规矩）
- PR #17（T2 README）:另一张 PR,同样不 merge,等用户 ack
- v0.3 spec 第 8 行的"已知漂移"声明在 PR #16 合并后即可移除（v0.4 后续 ticket 收尾时同步,本 PR 不动 v0.3 spec）
